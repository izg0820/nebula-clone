# 웹 콘솔 주기 설정 (선택 — Vite 빌드 타임 주입, 이상값은 기본값으로)
# 기기 목록 폴링 주기 (기본 5000, 최소 1000)
#VITE_DEVICE_POLL_INTERVAL_MS=5000
# 점유 keepalive 주기 (기본 30000) — 서버 NEBULA_OCCUPATION_TTL_MS보다 충분히 짧게
#VITE_KEEPALIVE_INTERVAL_MS=30000
# 입력 없을 때 keepalive 중단 상한 (기본 1800000=30분)
#VITE_KEEPALIVE_IDLE_LIMIT_MS=1800000
